# OpenTelemetry 기반 통합 관측성 아키텍처 검토

로그·트레이스·메트릭을 OpenTelemetry로 수집하고 VictoriaMetrics 생태계에 저장하는 통합 관측성 구성을 기술적으로 검토한 문서다. 기술적 제약(보안 정책, 네트워크 등)이 해소된 이상적 구성을 전제로, 구조의 장점과 단점, 확장성, 향후 대응 측면을 함께 다룬다.

## 설계 개요

핵심 아이디어는 세 가지다.

- **수집은 표준으로** — 로그·트레이스·메트릭을 모두 OpenTelemetry Collector로 수집해 OTLP로 표준화한다.
- **저장은 신호별 전용 백엔드로** — 신호마다 데이터 특성이 다르므로 전용 저장소를 둔다(로그: VictoriaLogs, 트레이스: VictoriaTraces, 메트릭: VictoriaMetrics).
- **조회·연결은 단일 창구로** — Grafana 한 화면에서 세 신호를 조회하고 `trace_id`로 상호 연결한다.

```
[다이어그램 1: 전체 아키텍처 개요]
```

각 애플리케이션 클러스터(dev/stg/prod)에서 OpenTelemetry가 세 신호를 수집해 중앙 클러스터(mgmt)로 전송하고, mgmt의 VictoriaMetrics 생태계가 저장, Grafana가 조회·알림을 담당하는 구조다.

## 세 신호의 수집 방식이 다르다

설계에서 가장 먼저 짚어야 할 점은 신호마다 수집 방식이 다르다는 것이다.

| 신호 | 생성 위치 | 수집 방식 | 앱 영향 |
|---|---|---|---|
| 로그 | 파드 표준출력 → 노드 파일(`/var/log/pods`) | Agent가 파일을 읽음 | 거의 없음 |
| 트레이스 | 앱 실행 중 생성(파일로 안 남음) | 앱 계측 후 OTLP 직접 전송 | **계측 작업 필요** |
| 메트릭 | 앱·인프라가 노출 | scrape 또는 OTLP 수신 | 노출 설정 필요 |

```
[다이어그램 2: 세 신호의 수집 경로 차이]
```

로그는 노드에 쌓인 파일을 긁어오면 되지만, 트레이스는 앱이 직접 내보내야 한다. 즉 트레이스를 도입하려면 애플리케이션 코드에 계측(instrumentation)을 넣는 선행 작업이 필요하다. 이건 뒤의 "단점·고려사항"에서 다시 짚는다.

## 수집 계층: OpenTelemetry Collector

### Agent와 Gateway 2단 구성

대규모에서는 Collector를 2단으로 배포한다. 둘은 같은 이미지이고 실행 모드만 다르다.

| 역할 | 배포 형태 | 기능 |
|---|---|---|
| Agent | DaemonSet(노드마다 1개) | 노드 로그 수집, 앱이 보낸 트레이스·메트릭 수신 |
| Gateway | Deployment(클러스터당 2~3개) | Agent들의 데이터를 모아 중앙으로 전송 |

```
[다이어그램 3: Agent/Gateway 2단 수집 구조]
```

Gateway를 두면 외부로 나가는 연결을 2~3개로 단일화해 네트워크·방화벽·재시도 관리가 단순해지고, 배치·버퍼링을 집중 처리할 수 있다. 소규모라면 Gateway 없이 Agent가 백엔드로 직접 보내도 된다.

### 하나의 Gateway가 세 신호를 분배

Gateway는 OTLP 입구(4317/4318)로 세 신호를 모두 받아, 신호별 파이프라인으로 나눠 각 전용 백엔드로 보낸다.

```
[다이어그램 4: Gateway 내부의 신호별 파이프라인 분기]
```

```yaml
service:
  pipelines:
    logs:    { receivers: [otlp, filelog], exporters: [victorialogs] }
    traces:  { receivers: [otlp],          exporters: [victoriatraces] }
    metrics: { receivers: [otlp],          exporters: [victoriametrics] }
```

이 구조의 의미는, 로그 파이프라인을 이미 운영 중이라면 트레이스·메트릭은 **같은 수집 인프라에 파이프라인만 추가**하면 된다는 것이다. 확장성 측면의 핵심이다.

## 저장 계층: VictoriaMetrics 생태계

세 신호를 동일 생태계의 전용 백엔드에 저장한다. 세 제품 모두 유사한 운영 방식(클러스터 구조, `vmauth` 진입점, `vmui` 내장 UI, 로컬 디스크 저장)을 공유한다.

| 신호 | 저장소 | 적재 진입점(클러스터) |
|---|---|---|
| 로그 | VictoriaLogs | `vmauth:8427/insert/opentelemetry/v1/logs` |
| 트레이스 | VictoriaTraces | `vmauth:8427/insert/opentelemetry/v1/traces` |
| 메트릭 | VictoriaMetrics | `vminsert`(OTLP/Prometheus) |

```
[다이어그램 5: mgmt 저장 계층 상세]
```

트레이스 백엔드는 VictoriaTraces 대신 Grafana Tempo도 선택지다. 이건 단순한 취향 문제가 아니라 트레이드오프가 있어서, 아래에서 별도로 비교한다.

## 멀티클러스터 중앙집중 구조

dev/stg/prod는 수집만, mgmt가 중앙 저장·조회를 맡는다.

```
[다이어그램 6: 멀티클러스터 중앙집중 토폴로지]
```

각 클러스터의 Gateway가 자기 환경 데이터를 mgmt로 보내고, `env` 라벨(dev/stg/prod)로 환경을 구분해 하나의 Grafana에서 분리 조회한다. 클러스터 간 전송은 TLS·인증(`vmauth`)을 적용한다.

## 조회와 신호 연결

### 데이터소스 구성

| 신호 | Grafana 데이터소스 |
|---|---|
| 로그 | VictoriaLogs(LogsQL) |
| 트레이스 | Jaeger(VictoriaTraces) 또는 Tempo |
| 메트릭 | Prometheus(VictoriaMetrics) |

### trace_id로 신호를 잇는다

이 구조의 실질적 가치는 신호 간 이동에 있다.

```
[다이어그램 7: trace_id 기반 신호 상관관계]
```

- 로그 → 트레이스: 에러 로그의 `trace_id` 클릭 → 그 요청의 전체 트레이스로 이동.
- 트레이스 → 로그: 느린 스팬 → 그 시점·서비스의 로그로 이동.
- 트레이스 → 메트릭: 서비스 그래프·스팬 메트릭으로 성능 추세 확인.

단, 이 연결은 로그와 트레이스가 같은 `trace_id`를 공유해야 가능하다. 앱 계측 시 `trace_id`를 로그에도 출력해야 하며, 안 되면 연결 자체가 성립하지 않는다.

## 고급 활용 요소

- **알림**: `vmalert`로 로그·메트릭 기반 알림 규칙 평가 → AlertManager 통지(예: 에러 로그 급증, p99 지연 초과).
- **샘플링**: 트레이스 전량 저장은 비용이 크므로 tail sampling으로 비율·조건 샘플링(에러·고지연은 보존, 정상은 비율 샘플링).
- **멀티테넌시**: `vmauth`의 AccountID/ProjectID로 팀·서비스별 데이터 격리.

```
[다이어그램 8: 고급 요소 포함 최종 구성도]
```

---

## 기술적 장점

**1. 벤더 종속이 없다.** 수집을 OpenTelemetry(CNCF 표준)로 통일하므로 백엔드를 바꿔도 수집 계층을 재사용한다. 나중에 VictoriaLogs를 다른 저장소로 교체하거나, 트레이스 백엔드를 Tempo↔VictoriaTraces로 바꿔도 exporter 설정만 손대면 된다.

**2. 운영 방식이 통일된다.** 세 신호 저장소가 모두 VictoriaMetrics 계열이라 클러스터 구조·진입점(`vmauth`)·UI(`vmui`)·알림(`vmalert`)이 같은 방식이다. 서로 다른 제품 세 개를 익히는 것보다 학습·운영 부담이 작다.

**3. 저장 비용이 낮다.** VictoriaMetrics 계열은 압축률이 높고, 로그·트레이스가 로컬 디스크 기반이라 별도 오브젝트 스토리지(S3/MinIO) 운영이 없다. 인프라 구성요소가 줄어든다.

**4. 신호가 연결된다.** `trace_id`로 로그·트레이스·메트릭을 오갈 수 있어 장애 원인 분석이 빨라진다. 세 신호를 따로 보는 것보다 실질적 가치가 크다.

**5. 점진 확장이 쉽다.** 같은 수집 인프라(Agent/Gateway)에 파이프라인·백엔드만 추가하는 구조라, 로그 → 메트릭 → 트레이스 순으로 단계적으로 확장하면서 앞 단계를 버리지 않는다.

## 기술적 단점·고려사항

**1. 트레이스는 공짜가 아니다.** 로그·메트릭과 달리 트레이스는 앱 계측이 선행되어야 한다. 언어별 SDK 적용 또는 자동계측 도입, `trace_id`를 로그에 함께 출력하는 작업이 필요하다. 서비스 수가 많으면 이 계측 작업의 공수가 작지 않다.

**2. VictoriaTraces는 상대적으로 신생이다.** VictoriaLogs/Metrics는 성숙도가 검증됐지만, VictoriaTraces는 비교적 최근에 나왔다. 기능 성숙도·레퍼런스·커뮤니티 측면에서 Grafana Tempo가 더 검증돼 있다. 생태계 일관성을 택하면 VictoriaTraces, 안정성·레퍼런스를 택하면 Tempo가 유리하다. 트레이스가 미션 크리티컬하다면 도입 시점에 VictoriaTraces 성숙도를 재확인하는 게 안전하다.

**3. mgmt 중앙집중은 단일 장애점이 될 수 있다.** 모든 환경의 데이터가 mgmt로 모이므로, mgmt 저장 계층 장애가 전 환경 관측성에 영향을 준다. HA 구성(컴포넌트 복제, 스토리지 이중화)과 mgmt 자체 모니터링이 필요하다. 환경 격리가 강하게 요구되면 환경별 분리 저장도 대안이지만, 그러면 통합 조회의 이점이 줄어든다.

**4. 카디널리티·비용 관리가 필요하다.** 로그 스트림 필드나 트레이스 양을 통제하지 않으면 저장·조회 성능과 비용이 빠르게 악화된다. 스트림 필드 제한, 트레이스 샘플링, retention 정책을 처음부터 설계해야 한다. "일단 다 저장"은 대규모에서 비용 문제로 돌아온다.

**5. 클러스터 간 네트워크 의존이 생긴다.** dev/stg/prod가 mgmt로 데이터를 보내므로 클러스터 간 네트워크 경로·방화벽·TLS가 전제된다. 이 경로가 막히면 수집이 끊긴다. 단일 클러스터 구성보다 네트워크 설계 부담이 크다.

## 트레이스 백엔드 비교: VictoriaTraces vs Tempo

| 항목 | VictoriaTraces | Grafana Tempo |
|---|---|---|
| 저장 방식 | 로컬 디스크(PVC) | 오브젝트 스토리지(S3/MinIO) 필수 |
| 생태계 | VictoriaMetrics 계열과 통일 | Grafana 계열과 통일 |
| 운영 부담 | 별도 스토리지 불필요 | 오브젝트 스토리지 운영 추가 |
| 성숙도 | 발전 중 | 검증됨 |
| Grafana 데이터소스 | Jaeger | Tempo |

생태계 일관성·운영 단순성을 우선하면 VictoriaTraces, 안정성·검증된 레퍼런스를 우선하면 Tempo가 합리적이다.

## 향후 대응·확장성 평가

**확장성은 높다.** 신호 추가(로그→메트릭→트레이스)나 클러스터 추가가 기존 인프라를 재사용하는 구조다. 수집 계층이 OpenTelemetry 표준이라, 새로운 백엔드나 도구가 나와도 OTLP를 지원하는 한 갈아끼울 수 있다. 수평 확장(저장 노드 증설)도 VictoriaMetrics 계열의 클러스터 모드로 대응된다.

**기술 트렌드와도 부합한다.** 관측성 업계는 OpenTelemetry 표준 수집과 신호 통합으로 수렴하는 흐름이다. 이 구조는 그 방향에 올라타 있어, 향후 인력 채용·기술 문서·커뮤니티 지원 측면에서도 유리하다.

**리스크는 두 가지를 주시해야 한다.** 하나는 VictoriaTraces의 성숙도 진행 상황, 다른 하나는 규모 증가 시 카디널리티·비용이다. 둘 다 설계 단계에서 대비 가능한 항목이며, 트레이스 백엔드는 표준(OTLP) 위에 있어 필요 시 Tempo로 전환하는 퇴로도 열려 있다.

**종합하면**, 표준 기반이라 향후 변화에 유연하고 확장 경로가 명확한 구조다. 단점은 대부분 "신생 컴포넌트 성숙도"와 "대규모 운영 관리"에 관한 것으로, 표준을 택한 덕분에 대안 전환이 가능하다는 점이 안전판이 된다.

## 단계적 도입 제안

1. **로그** — VictoriaLogs + OTel(로그). 가장 기반이 되는 신호.
2. **메트릭** — VictoriaMetrics 추가. 시스템·앱 상태 가시화.
3. **트레이스** — 앱 계측 + VictoriaTraces(또는 Tempo) 추가.
4. **연결·고도화** — `trace_id` 상관관계, 알림, 샘플링, 멀티테넌시.

각 단계가 OpenTelemetry 수집 계층을 재사용하므로, 인프라를 새로 짜지 않고 파이프라인·백엔드만 추가한다.

## 핵심 포트 참고

| 구분 | 포트 |
|---|---|
| OTLP gRPC | 4317 |
| OTLP HTTP | 4318 |
| vmauth(클러스터 진입점) | 8427 |
| VictoriaLogs(single) | 9428 |
| VictoriaTraces(single) | 10428 |
